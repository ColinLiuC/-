	/*//获取市民云授权返回的code值	
	$(document).ready(function(){
	    var code = queryString(code);		
		GetUserInfoAuth(code);
	});

    //对接市民云用户
	function GetUserInfoAuth(code) {
		$.web_Post('AppUser/getUserInfoAuth', {
			code: code
		}, onSuccess_GetUserInfoAuth, 1);
	}

	 var onSuccess_GetUserInfoAuth = function(data) {
		if(data.msg == "请登录") {
			window.location.href = "../User/login.html";
		} else {
			var userobj = {
				userid: data.userId,
				username: data.userName,
				realname: data.realName,
				phone: data.phone,
				idcart: data.idCard
			};
			var loginuser = JSON.stringify(userobj);
			localStorage.setItem("loginuser", loginuser);
		}
	}*/